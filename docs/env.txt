$ virtualenv venv
$ . venv/bin/activate
$ python --version
Python 3.6.3

$ pip --version
pip 9.0.1 from <project>/venv/lib/python3.6/site-packages (python 3.6)

$ pip install Flask
$ mkdir tamuro
$ touch tamuro/__init__.py
$ code tamuro/server.py
$ export FLASK_APP=tamuro/server.py
$ flask run
[Ctrl]+[C]

$ mkdir ${PWD}/tmp
$ mkdir ${PWD}/tmp/db
$ /usr/local/pgsql/bin/initdb -D ${PWD}/tmp/db
$ /usr/local/pgsql/bin/pg_ctl -D ${PWD}/tmp/db -l ${PWD}/tmp/db/log.txt start
$ /usr/local/pgsql/bin/createdb test
$ /usr/local/pgsql/bin/psql test
$ /usr/local/pgsql/bin/pg_ctl -D ${PWD}/tmp/db  stop

$ code setup.py
$ pip install -e .

$ rm **/__pycache__/*

$ export FLASK_APP=tamuro
$ flask run

$ echo drop schema test cascade | psql test
$ echo drop schema test cascade | psql test ; \
   coverage run setup.py test; \
   coverage report -i -m tamuro/* tamuro/*/*
