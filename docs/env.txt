$ mkdir ${PWD}/tmp
$ mkdir ${PWD}/tmp/db
$ initdb -D ${PWD}/tmp/db
$ pg_ctl -D ${PWD}/tmp/db -l ${PWD}/tmp/db/log.txt start
$ createdb test --encoding=UTF8 --locale=ja_JP.UTF-8
$ psql test
# create user test password 'test';
# GRANT ALL PRIVILEGES ON DATABASE test TO test;
# \q
$ psql test -U test -W
$ pg_ctl -D ${PWD}/tmp/db stop
