export const defaults = {
  chatSummaryExpand: true,
  darkTheme: false,
  locale: 'ja_JP',
  menuPosition: 'br',
  tz: 'Asia/Tokyo',
  waitProcTimeout: 10 * 1000,
  routeExcludeFromStorage: [
    'invitation',
    'signin',
    'policy'
  ]
}
