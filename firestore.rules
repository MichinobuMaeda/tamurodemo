rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isAdmin() {
      return request.auth.uid in get(/databases/$(database)/documents/groups/admin).users;
    }
    function isManager() {
      return request.auth.uid in get(/databases/$(database)/documents/groups/manager).users;
    }
    match /service/status {
      allow write: if isAdmin();
      allow read: if true;
    }
    match /groups/{document=**} {
      allow write: if isManager();
      allow read: if request.auth.uid != null;
    }
    match /users/{userId} {
      allow read, write: if (request.auth.uid == userId) || isManager();
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}