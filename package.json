{
  "name": "tamuro.api",
  "version": "0.3.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "forever-start": "forever start --append --uid \"tamuro.api.test\" lib",
    "forever-stop": "forever stop \"tamuro.api.test\"",
    "build": "babel src --out-dir lib --ignore __tests__ --source-maps",
    "test": "npm run build; jest --coverage --no-cache --runInBand",
    "test-start": "export NODE_ENV=development; npm run build; npm run forever-start; npm run setup",
    "start": "npm run build; npm run forever-start",
    "stop": "npm run forever-stop",
    "setup": "npm run build; node lib/setup.js"
  },
  "author": "Michinobu Maeda",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-jest": "^20.0.3",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^4.2.0",
    "superagent": "^3.6.0",
    "superagent-prefix": "^0.0.2"
  },
  "dependencies": {
    "continuation-local-storage": "^3.2.0",
    "google-auth-library": "^0.11.0",
    "koa": "^2.3.0",
    "koa-bodyparser": "^4.2.0",
    "pg": "^7.1.2",
    "pg-hstore": "^2.3.2",
    "sequelize": "^4.5.0",
    "shortid": "^2.2.8"
  }
}
